const math = require("./math");

//const math = require(".fs") This was a built in module which comes with package

// const {add, sub} = require("./math"){Destructuring}

// console.log(addFn(3, 6)) after destructuring we can write this way too
console.log(math.addFn(2, 6));
